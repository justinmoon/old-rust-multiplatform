[package]
name = "counter"
version = "0.1.0"
edition = "2021"

[lib]
crate_type = ["cdylib", "staticlib"]
name = "counter"

[dependencies]
crossbeam = "0.8.4"
once_cell = "1.19.0"
uniffi = { version = "0.27.2", features = ["cli"] }

# [build-dependencies]
# uniffi = { version = "0.27.2", features = ["build"] }
